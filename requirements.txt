# Core dependencies
pydantic>=2.0
pyyaml>=6.0
requests>=2.31
python-dotenv>=1.0
tenacity>=8.0
openai>=1.0
rdflib>=6.0
jsonschema>=4.0
linkml>=1.8

# PDF processing
marker-pdf>=1.10

# Ontology agent (use existing package)
# ontology-agent installed separately from ./

# For data validation and serialization
# Already included via pydantic
